/* instructor created
handle the loop
the diver
call student funcs 
main func 
when you land on a tile, engine passes the whole player struct into student funcs 
type influences movement - if type x then move two instead */