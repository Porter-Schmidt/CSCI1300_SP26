#include<iostream>

using namespace std;

int main() {
    // // 1
    // string word = "HelloWorld";
    // cout << word.substr(0, 5);

    // // 2
    // string s = "abcdef";
    // cout << s.substr(2, 3);

    // // 3
    // string course = "CSCI1300";
    // cout << course.substr(4, course.length() - 3);

    // // 4
    // string course = "CSCI1300";
    // cout << course.substr(course.length(), 1);

    // // 5
    // string s = "0123456789";
    // cout << s.substr(s.substr(2,3).length(), 4);

    // // 6
    // string s = "ABCDEFGHIJ";
    // cout << s.substr(s.length() / 2);

    // // 7
    // string s = "computer";
    // cout << s.substr(-1);

    // // 8
    // string s = "hello";
    // cout >> s.substr(1/2);

    // // 9
    // string s = "hello";
    // cout << s.substr(1, s.length());

    // 10
    string s = "hello";
    cout << s.at(8);

    return 0;
}


